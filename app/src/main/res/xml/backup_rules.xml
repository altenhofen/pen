<?xml version="1.0" encoding="utf-8"?><!--
    Android 11 and older. These releases have no way to require client-side encryption, so the
    allowlist below carries the motor sliders and nothing else. Handwriting prototypes and word
    samples stay on the device; the user moves them with an encrypted export instead.
-->
<full-backup-content>
    <include domain="file" path="datastore/motor_settings.preferences_pb" />
</full-backup-content>
